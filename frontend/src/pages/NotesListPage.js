import React, {useState, useEffect} from 'react'

const NotesListPage = () => {

  let [notes, setNotes] = useState([]);

  useEffect(()=>{

  },[])

  let getNotes = async () => {
    fetch()
  }
  return (
    <div>
      notes
    </div>
  )
}

export default NotesListPage
